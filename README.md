# iphone-sns-button
Simple mobile application that runs on pythonista that triggers AWS SNS notification with custom message. Set up .env file with SNS endpoint, custom message, and CSV file with list of notified contacts.
